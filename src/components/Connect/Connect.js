import React, { forwardRef } from 'react'
import './Connect.css'

const Connect = forwardRef((props, ref) => {
  return (
    <div className='connect' ref={ref}>Connect</div>
  )
})

export default Connect